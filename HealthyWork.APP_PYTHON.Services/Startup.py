# import __init__.py
from package import *
import requests, json

# Main class
def main():

    # try login in the service
    access = login.login.Run()
    
    # if login is correct
    if access == true:

        # loop
        while True:
            try:

            except:
                # poner log
